// eslint-disable-next-line no-unused-vars
import React from "react"

const Container = ({ children }) => {

  return (
    <div className={`container mx-responsive`}>{ children }</div>
  )
}

export default Container

